# Required only when using the optional Firebase Phase 1 connection.
FIREBASE_PROJECT_ID=lifexp-d4e68
# Windows example (use your real local path; never commit the JSON key):
# GOOGLE_APPLICATION_CREDENTIALS=C:\\path\\to\\firebase-credentials.json
